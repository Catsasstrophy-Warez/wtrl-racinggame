# Wrench to Race Legends Product Truth

## Authoritative revision

**Swift Rev32 Durable Components + Ford Continuum + RealityKit Service RC**, derived from Rev31.

## Current executable scope

153 heritage vehicles across 15 lineages retain generation-specific deep assemblies. Rev32 adds durable component-instance bindings so an assembly node is a physical location while `componentInstanceId` identifies the replaceable object occupying it. Service interfaces bind to those actual component instances, and RealityKit commands carry both component and assembly identities.

The 27-car Ford Triple-Wide Continuum now has three explicit simulation doctrines: Heavy Iron/Pursuit, Homologation/Rally, and Aerospace/Endurance. These route into the existing executable fault primitives and distinct diagnostic families.

## Research gate

Historical/OEM names and supplied engineering details remain reference inputs. Exact dimensions, torque values, clearances, pressures, power ratings, aero values, option-specific hardware and calibration maps require independent verification and provenance before becoming production truth.

## Validation truth

Linux/Swift 6.2.1 platform-neutral proof: 171 Swift Testing + 83 XCTest = 254 executable tests, zero failures. Dedicated Rev32 validator: 6/6. Apple-specific Xcode SDK, Simulator, RealityKit, Metal, XCUITest, Instruments, signing and physical-device execution remain outstanding.

## Historical product-truth notes


# Wrench to Race Legends Product Truth

## Authoritative revision

**Swift Rev26 Physical Diagnosis, Repair & Verification development build**, derived directly from the verified Rev25 Persistent Mechanical Truth RC.

Rev25 remains the immutable incoming baseline. Its ZIP SHA-256 was `0da52a04989a736823d157f189e57f1af1aea25721469e81557afbca4e8c14`.

## Architecture

WTRL is a native Swift package targeting iOS 18+ and macOS 15+. Unity is historical/reference material only and is not a production architecture authority.

The production direction remains Swift / SwiftUI / RealityKit / Metal / AVFoundation. Platform-neutral Linux validation covers only code that can execute there.

## Rev26 physical truth

Rev26 extends the persistent mechanical truth rather than replacing it. Impact consequences can become addressable component condition, alignment state and workshop evidence. Repair, alignment and calibration mutate the same persisted truth, and verification compares before/after measurements instead of equating part installation with success.

## Research gate

Historical/OEM values, production identities, torque specifications and calibration values are not considered authoritative merely because they appear in content. They require independent verification and provenance before being promoted to production truth. The Three-Change Rule is not legal clearance.

## Apple validation status

Not proven by Linux: Xcode app build, SwiftUI rendering, RealityKit, Metal, AVFoundation, XCUITest, Instruments, code signing, Simulator execution, thermal/device performance and physical-iPhone behavior.
